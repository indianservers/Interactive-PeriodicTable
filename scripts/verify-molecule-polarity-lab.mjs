import{createRequire}from'node:module';import{mkdir,writeFile}from'node:fs/promises';import assert from'node:assert/strict';
const root=process.env.PLAYWRIGHT_MODULE_ROOT;if(!root)throw Error('Set PLAYWRIGHT_MODULE_ROOT.');
const{chromium}=createRequire(`${root}/package.json`)('playwright'),base=process.env.CHEMISTRY_VL_URL||'http://127.0.0.1:5174/?screen=home#/simulations/molecule-polarity',out='docs/chemistry-vl-mockup-rebuild/screenshots/molecule-polarity';
await mkdir(out,{recursive:true});const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'}),errors=[],layouts=[],screens=[];
try{
 const page=await browser.newPage({viewport:{width:1672,height:941}});page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});
 await page.goto(base,{waitUntil:'networkidle'});await page.getByRole('button',{name:'Pause animations'}).evaluate(x=>x.style.display='none');
 await page.getByRole('button',{name:'Start with H₂O'}).click();assert.match(await page.locator('.analysis').innerText(),/ΔEN = 0\.96[\s\S]*1\.08 D[\s\S]*21%/);
 await page.getByRole('slider',{name:'Bond length'}).fill('140');assert.match(await page.locator('.analysis').innerText(),/1\.19 D/);await page.getByRole('button',{name:'Add to Comparison'}).click();await page.getByRole('button',{name:'Next: Geometry'}).click();
 await page.getByRole('button',{name:'CO₂ Linear'}).click();assert.match(await page.getByRole('heading',{name:'Vector addition'}).locator('..').innerText(),/Calculated resultant[\s\S]*0\.00 D/);await page.getByRole('button',{name:'Next'}).click({force:true});
 await page.getByRole('button',{name:'Polar',exact:true}).click();assert.match(await page.locator('.challenge').innerText(),/Correct/);await page.getByRole('button',{name:'Next'}).click({force:true});
 await page.getByRole('button',{name:/Record Data/}).click();assert.match(await page.locator('.field-controls').innerText(),/Reading recorded/);await page.getByRole('button',{name:'Next'}).click({force:true});
 await page.getByRole('button',{name:/Molecular shape/}).click();await page.getByRole('button',{name:/permanent dipole/}).click();assert.match(await page.locator('.quiz').innerText(),/Score: 3\/3/);
 for(const id of['home','builder','geometry','comparison','field','report']){await page.goto(base.replace('screen=home',`screen=${id}`),{waitUntil:'networkidle'});await page.locator('[role="status"],button[aria-label="Pause animations"]').evaluateAll(n=>n.forEach(x=>x.style.display='none'));await page.screenshot({path:`${out}/${id}-1672.png`,fullPage:true});screens.push({id,passed:true})}
 for(const[width,height]of[[1440,900],[1024,768],[768,1024],[390,844]]){await page.setViewportSize({width,height});await page.goto(base.replace('screen=home','screen=geometry'),{waitUntil:'networkidle'});await page.locator('[role="status"],button[aria-label="Pause animations"]').evaluateAll(n=>n.forEach(x=>x.style.display='none'));const metric=await page.evaluate(()=>({width:innerWidth,height:innerHeight,scrollWidth:document.documentElement.scrollWidth,overflow:document.documentElement.scrollWidth>innerWidth+1}));assert.equal(metric.overflow,false);layouts.push(metric);await page.screenshot({path:`${out}/geometry-${width}.png`,fullPage:true})}
 assert.deepEqual(errors,[]);await writeFile(`${out}/verification.json`,JSON.stringify({simulator:'molecule-polarity',passed:true,screens,layouts,consoleErrors:errors},null,2));console.log('ALL MOLECULE POLARITY CHECKS PASSED')
}finally{await browser.close()}
