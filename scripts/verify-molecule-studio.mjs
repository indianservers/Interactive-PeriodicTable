import{createRequire}from'node:module';import{writeFile}from'node:fs/promises';import assert from'node:assert/strict';const root=process.env.PLAYWRIGHT_MODULE_ROOT;if(!root)throw Error('Set PLAYWRIGHT_MODULE_ROOT.');const{chromium}=createRequire(root+'/package.json')('playwright');const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',args:['--enable-webgl','--use-angle=swiftshader']});try{const page=await browser.newPage({viewport:{width:1920,height:1080}}),errors=[],warnings=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());if(m.type()==='warning')warnings.push(m.text());});await page.goto('http://127.0.0.1:5175/#molecule');await page.waitForTimeout(1200);assert.equal(await page.locator('.edit-mode').count(),1);assert.equal(await page.locator('canvas').count()>0,true);await page.getByRole('button',{name:'Presets',exact:true}).click();await page.getByRole('button',{name:/Create new molecule/}).click();await page.getByRole('button',{name:/Oxygen/}).first().click();assert.match(await page.locator('.ms-title').innerText(),/CO/);await page.getByRole('button',{name:'Inspect',exact:true}).click();await page.locator('.molstar-viewer[data-ready=true]').waitFor({timeout:60000});assert.equal(await page.locator('.inspect-mode').count(),1);assert.match(await page.locator('.molstar-viewer-badge').innerText(),/CUSTOM MOLECULE/);const canvas=page.locator('.molstar-viewer-host canvas').first(),initial=await canvas.screenshot();await page.getByRole('button',{name:'Space filling',exact:true}).click();await page.waitForTimeout(450);assert(!initial.equals(await canvas.screenshot()),'inspect representation changes');await page.screenshot({path:'docs/molecule-studio-review/after-inspect-1920.png'});await page.getByRole('button',{name:'Edit',exact:true}).click();assert.equal(await page.locator('.edit-mode').count(),1);assert.equal(await page.locator('.ms-canvas').count(),1);const layouts=[];for(const[width,height]of[[1920,1080],[1024,768],[390,844]]){await page.setViewportSize({width,height});await page.waitForTimeout(250);const layout=await page.evaluate(()=>({width:innerWidth,height:innerHeight,scrollWidth:document.documentElement.scrollWidth,scrollHeight:document.documentElement.scrollHeight}));layouts.push(layout);await page.screenshot({path:`docs/molecule-studio-review/layout-${width}.png`,fullPage:width<1200});}assert.deepEqual(errors,[]);await writeFile('docs/molecule-studio-review/verification.json',JSON.stringify({passed:true,layouts,errors,warnings},null,2));console.log('ALL MOLECULE STUDIO CHECKS PASSED');}finally{await browser.close();}
