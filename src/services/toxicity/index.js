export const toxicityEvidencePolicy = Object.freeze({
  missingMeasuredEndpoint: null,
  displayLabel: "Unavailable — no measured endpoint in the cached source set",
  allowPoseFabrication: false,
});

